syntax = "proto3";

package iams.agent;

import "google/protobuf/empty.proto";
// import "simulation.proto";

/*
*/
service Agent {
    // /*
    // Receive scheduled events in simulation run
    // */
    // rpc simulation(SimulationData) returns (stream SimulationResponse) {}

    /*
    Receive scheduled events in simulation run
    */
    rpc resume_simulation(SimulationData) returns (google.protobuf.Empty) {}

    // /*
    // Receive scheduled events in simulation run
    // */
    rpc ping(google.protobuf.Empty) returns (google.protobuf.Empty) {}
}

message SimulationData {
    bytes uuid = 1;
    float time = 2;
}
